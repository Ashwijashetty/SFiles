package com.cg.sa.Trial.service;

import org.springframework.data.mongodb.core.MongoTemplate;
import org.springframework.stereotype.Service;

import com.cg.sa.Trial.model.Student;

@Service
public class TrialService implements TrialServiceI {
	/*private static MongoTemplate mongoTemplate;
public TrialService (MongoTemplate mongoTemplate){
	this.mongoTemplate=mongoTemplate;
}
	public Student createData(Student student) {
		mongoTemplate.insert(student);
		return student;
	}

	*/
}
